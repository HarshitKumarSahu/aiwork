<%- include('partials/header') %>
<div class="postcreatecontainer">
    <div class="createcont">
        <h1>Upload Art</h1>
        <div class="formContainer">
            <div class="left">
                <!-- Image preview container -->
                <div id="imagePreviewContainer">
                    <h4>Preview:</h4>
                    <div class="imgcontainer">
                        <img id="imagePreview" src="" alt="Preview" style="display: none;">
                    </div>
                </div>
            </div>
            <div class="right">
                <form id="uploadPostForm" action="/createpost" method="post" enctype="multipart/form-data">
                    <input type="hidden" name="imageOption" value="upload"> <!-- Hidden field to indicate upload -->
        
                    <input type="file" name="postimage" id="postimage" accept="image/*" required onchange="previewImage(event)">
                    
                    <input type="text" placeholder="Title" name="title" required>
                    <textarea name="description" placeholder="Description" required></textarea>
                    <input class="submit" type="submit" value="Create Post">
                </form>
                <p>Want to try AI Art</p>
                <!-- <a class="lastbutton" href="/register">Create new account</a> -->
                <a class="lastbutton" href="/generate"><input class="submit" type="submit" value="Generate with AI"></a>
            </div>
        </div>
    </div>
</div>

<script>
function previewImage(event) {
    const input = event.target;
    const previewContainer = document.getElementById('imagePreviewContainer');
    const preview = document.getElementById('imagePreview');

    if (input.files && input.files[0]) {
        const reader = new FileReader();

        reader.onload = function(e) {
            preview.src = e.target.result;
            preview.style.display = 'flex';
        };

        reader.readAsDataURL(input.files[0]);
    } else {
        previewContainer.style.display = 'none';
    }
}
</script>

<%- include('partials/footer') %>
