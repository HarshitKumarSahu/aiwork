<!-- <%- include('partials/header') %>
    <h1>pins</h1>
    <div class="cards">
        <% user.post.forEach(elem => { %>
            <div class="card">
                <div style="overflow: hidden; width: 200px; height: 300px;">
                    <img class="profileImage" src="/images/uploads/<%=elem.image%>" alt="" style="width: 100%; height: 100%;">
                </div> -->
                <!-- <a href="/show/posts"><h2>all pins</h2></a> -->
                <!-- <h3><%= elem.title %></h3>
                <button class="downloadBtn" style="margin-top: 10px; padding: 8px 12px; cursor: pointer;">Download Image</button>
            </div>
            
        <% }); %>
    </div>
    <script>
        document.querySelectorAll('.downloadBtn').forEach((btn, index) => {
            btn.addEventListener('click', function () {
                const imageUrl = document.querySelectorAll('.profileImage')[index].src;
                const a = document.createElement('a');
                a.href = imageUrl;
                a.download = `profile_image_${index + 1}.jpg`; // Unique filename for each image
                document.body.appendChild(a);
                a.click();
                document.body.removeChild(a);
            });
        });
    </script>
<%- include('partials/footer') %> -->





// remove